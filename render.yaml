services:
  - type: web
    name: budgetai
    runtime: python
    plan: free

buildCommand: |
      pip install --upgrade pip
      pip install --prefer-binary -r requirements.txt

    startCommand: gunicorn app:app

    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
